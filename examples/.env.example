# Optional env vars recognised by tunnelmole-service.
# Copy this file to .env and set values as needed before running with Docker Compose.

# Set to "debug" to enable verbose request/response logging
TUNNELMOLE_LOG_LEVEL=

# Set to any non-empty value to log incoming connection info
LOG_CONNECTION_INFO=
